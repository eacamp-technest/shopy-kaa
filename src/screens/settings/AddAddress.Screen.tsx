import {StyleSheet, Text, View} from 'react-native';
import React from 'react';

export const AddAddressScreen = () => {
  return (
    <View>
      <Text>AddAddress.Screen</Text>
    </View>
  );
};

const styles = StyleSheet.create({});
